# Payments & Subscriptions

## Overview
Chip has four distinct payment types. There is no platform subscription — users only pay for specific Server-related actions.

| Payment type | Who pays | When | Recurrence |
|---|---|---|---|
| Investor one-off | Any `free` user | To gain `investor` role and create a Server | One-off |
| Server feature subscription | `investor` or `super_owner` | Monthly, per active Feature on their Server | Monthly |
| Server boost | Any registered user | To support a Server | One-off |
| Server monthly subscription | Any registered user | Sustained supporter access on a specific Server | Monthly |

---

## Payment Provider
- **Stripe** is the payment provider (Laravel Cashier for subscriptions).
- One-off payments use Stripe Payment Intents.
- Recurring subscriptions use Stripe Subscriptions (via Cashier).

---

## 1. Investor One-Off Payment

### Purpose
Unlocks `investor` platform role, enabling the user to create one Server.

### Amount
Fixed, set by `super_owner` in platform config. Value TBD.

### Flow
1. `free` user selects "Become an Investor" from their account.
2. Stripe Payment Intent created server-side.
3. User completes payment via Stripe Elements.
4. Stripe `payment_intent.succeeded` webhook fires.
5. User platform role updated to `investor`.
6. User is prompted to create and name their Server.

### Data Model
```
payments
  id                  ulid
  user_id             FK -> users
  type                enum: investor_upgrade | server_boost | server_subscription_once
  stripe_payment_id   string
  amount_pence        integer
  currency            string  default GBP
  status              enum: pending | succeeded | failed | refunded | partial_refund
  server_id           FK -> servers nullable   (for boost payments)
  refund_amount_pence integer nullable
  created_at / updated_at
```

---

## 2. Server Feature Subscription

### Purpose
The Investor pays monthly for each Feature active on their Server. Monthly cost = sum of each active Feature's `monthly_price_pence` (as configured by `super_owner`).

### Credit Pool Offset
If the Server's `credit_balance` has funds, they are applied to the bill automatically before charging the Investor's payment method. Only the remainder is charged.

### Flow
1. Investor subscribes a Feature to their Server from the Server billing dashboard.
2. Stripe Subscription Item created (or updated) via Cashier.
3. Monthly invoice generated by Stripe.
4. Credit pool offset applied before charge.
5. `invoice.payment_succeeded` webhook updates subscription status.
6. `invoice.payment_failed` webhook: notify Investor; grace period TBD.

### Data Model (via Cashier)
Cashier manages `subscriptions` and `subscription_items` tables. The app adds:
```
users
  stripe_id             string nullable   (Stripe Customer ID)
  pm_type               string nullable
  pm_last_four          string nullable
  trial_ends_at         timestamp nullable
```

Server-level subscription state is tracked in `server_features` (see `blueprint/roles-permissions.md`).

---

## 3. Server Boost (One-Off)

### Purpose
Any registered user pays a one-off amount to support a specific Server. Boosts the Server's credit pool and grants `supporter` server role to the payer on that Server.

### Amount Rules
- Minimum: £0 (symbolic boost — grants supporter role, no credit).
- Maximum: set per-feature context by the Server owner, up to the platform-wide max set by `super_owner`.
- The Server owner sets the boost amounts visible to supporters.

### Flow
1. User chooses a boost amount from options the Server owner has configured.
2. Stripe Payment Intent created.
3. On `payment_intent.succeeded`:
   - `credit_balance` on the Server incremented.
   - `server_credit_transactions` row inserted (type: `boost_in`).
   - User's `server_members` role updated to `supporter` (if not already higher).
   - Access rules evaluated — Server owner's configured grants for supporters are applied.

### Refund Policy
- 80% of the original payment amount.
- Refund must be requested within TBD days of payment.
- Legal details TBD.
- Refund recorded in `payments` (`status: partial_refund`, `refund_amount_pence` set).

---

## 4. Server Monthly Subscription

### Purpose
A registered user takes out a recurring monthly subscription to a specific Server for sustained `supporter` access.

### 14-Day Cancellation Right Waiver
- EU/UK consumer law provides a 14-day right to cancel a digital subscription and receive a full refund (the "cooling-off period").
- At subscription signup and on **every auto-renewal**, the user must explicitly waive this right by confirming they want immediate access to digital content.
- The waiver is recorded with a timestamp for each renewal.
- Without the waiver, the 14-day right applies and the user could claim a full refund.

### Waiver Data Model
```
subscription_waivers
  id                  ulid
  user_id             FK -> users
  server_id           FK -> servers
  stripe_invoice_id   string
  waived_at           timestamp
  period_start        timestamp
  period_end          timestamp
```

### Flow
1. User selects a monthly subscription option on the Server.
2. Waiver confirmation shown: "I confirm I want immediate access and waive my 14-day cancellation right."
3. User accepts waiver → Stripe Subscription created.
4. On each `invoice.payment_succeeded`: new waiver row inserted for that billing period.
5. On cancellation or lapse: `supporter` server role retained until period end; access removed after.

---

## Credit Pool Investment

Server owner invests `credit_balance` from the Server dashboard:

| Investment type | Effect |
|---|---|
| Subscribe/renew a Feature | Deducts from `credit_balance`; activates `server_features` row |
| Purchase Member Slots | Deducts from `credit_balance`; increments `servers.member_cap` |
| Bill offset | Deducted automatically from next Stripe invoice |

All investments are recorded in `server_credit_transactions`.

Rules:
- Credits are locked to the Server — no cash out.
- Credits cannot be transferred between Servers.
- If a Server is deleted (out of scope for now), credit handling TBD.

---

## Webhook Events to Handle

| Stripe event | Action |
|---|---|
| `payment_intent.succeeded` | Investor upgrade OR boost payment processed |
| `customer.subscription.created` | Server feature subscription or Server monthly subscription active |
| `customer.subscription.updated` | Feature added/removed; sync `server_features` |
| `customer.subscription.deleted` | Subscription cancelled; schedule access revocation |
| `invoice.payment_succeeded` | Record renewal; insert waiver row for Server subscriptions |
| `invoice.payment_failed` | Notify user; start grace period (TBD) |

---

## Development & Testing

See `blueprint/development-setup.md` for the full test checklist, Stripe test cards, and webhook forwarding setup.

### Key scenarios to test
- Investor upgrade: successful payment → role change → Server creation prompt.
- Server boost: payment → credit pool increment → supporter role granted.
- Monthly subscription: signup with waiver → auto-renewal with new waiver record.
- Failed renewal: notification received; grace period behaviour.
- Refund: 80% returned; `payments` record updated.
- Credit offset: Server bill reduced by available credits before charge.

---

## Open Questions
- What is the Investor one-off payment amount? (TBD, set by `super_owner` in config)
- What is the grace period when an Investor's Server bill cannot be paid?
- What is the refund request window (days after payment)?
- Can credits be inherited if an Investor transfers Server ownership (out of scope v1)?
- Can a user hold monthly subscriptions to multiple Servers simultaneously?
